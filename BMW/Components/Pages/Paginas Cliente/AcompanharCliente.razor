@page "/encomendas/acompanhar/{id:int}"

<PageTitle>Acompanhar Encomenda</PageTitle>

<TableLayout 
    UserName="Diogo Fernandes"
    UserRole="Cliente"
    Button1Text="Encomendas"
    Button1Href="/encomendas"
    Button1Icon="📦"
    Button2Text="Catálogo"
    Button2Href="/catalogo"
    Button2Icon="🛒"
    Title="Acompanhar Encomenda">

    <div class="encomenda-container">
        <div class="encomenda-detalhes">
            <h3 class="encomenda-titulo">Encomenda <a href="#">#@Id</a></h3>
            <p><strong>Veículo:</strong> @Modelo</p>
            <p><strong>Data da Encomenda:</strong> @Data</p>
            <p><strong>Estado Atual:</strong> @Estado</p>
            <p><strong>Fase Atual:</strong> @FaseAtual</p>
        </div>

        <div class="encomenda-imagem">
            <img src="images/bmw-montagem-cliente.jpg" alt="Montagem do Veículo" />
        </div>

        <div class="encomenda-progresso">
            <p class="encomenda-progresso-texto"><strong>Progresso da Montagem:</strong></p>
            <div class="progress-bar">
                @for (int i = 1; i <= TotalFases; i++)
                {
                    <span class="progress-step @(i <= FaseAtualIndex ? "completed" : "")"></span>
                }
            </div>
        </div>
    </div>
</TableLayout>

@code {
    [Parameter]
    public int Id { get; set; }

    private string Modelo = "BMW i7";
    private string Data = "12-09-2024";
    private string Estado = "Em Produção";
    private string FaseAtual = "Instalação do Sistema de Arrefecimento";
    private int FaseAtualIndex = 4; // Exemplo de fase atual
    private int TotalFases = 7;    // Total de fases do processo
}

<style>
    .encomenda-container {
        padding: 20px;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
        height:92%;
        color: #121212; /* Cor do texto ajustada para preto ou quase preto */
    }

    .encomenda-detalhes {
        margin-bottom: 20px;
    }

    .encomenda-detalhes h3 {
        margin-bottom: 10px;
        color: #007bff; /* Azul para destaque no título */
    }

    .encomenda-detalhes p {
        margin: 5px 0;
        color: #333333; /* Cinza escuro para contraste */
    }

    .encomenda-imagem img {
        width: 100%;
        max-width: 600px;
        border-radius: 10px;
        margin: 20px 0;
    }

    .encomenda-progresso {
        margin: 20px 0;
    }

    .encomenda-progresso-texto {
        margin-bottom: 10px;
        color: #333333; /* Cinza escuro */
    }

    .progress-bar {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }

    .progress-step {
        width: 15px;
        height: 15px;
        border-radius: 50%;
        background-color: #cccccc;
    }

    .progress-step.completed {
        background-color: #007bff;
    }
</style>
