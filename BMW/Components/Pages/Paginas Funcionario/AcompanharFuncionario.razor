@page "/funcionario/gestor-encomendas/acompanhar/{id:int}"

<PageTitle>Acompanhar Encomenda</PageTitle>

<TableLayout 
    UserName="Diogo Fernandes"
    UserRole="Funcionário"
    Button1Text="Gestão de Encomendas"
    Button1Href="/funcionario/gestor-encomendas"
    Button1Icon="📦"
    Button2Text="Relatórios"
    Button2Href="/funcionario/relatorios"
    Button2Icon="📄"
    Title="Acompanhar Encomenda">

    <div class="encomenda-container">
        <div class="encomenda-detalhes">
            <h3 class="encomenda-titulo">Encomenda <a href="#">#@Id</a></h3>
            <p><strong>Veículo:</strong> @Modelo</p>
            <p><strong>Fase de Montagem Atual:</strong> @FaseAtual</p>
        </div>

        <div class="encomenda-imagem">
            <img src="images/bmw-montagem.jpg" alt="Montagem de Veículo" />
        </div>

        <div class="encomenda-progresso">
            <p><strong>Progresso:</strong></p>
            <div class="progress-bar">
                @for (int i = 1; i <= TotalFases; i++)
                {
                    <span class="progress-step @(i <= FaseAtualIndex ? "completed" : "")"></span>
                }
            </div>
        </div>

        <div class="encomenda-acoes">
            <button class="btn btn-primary">Registar Fase de Montagem</button>
            <button class="btn btn-primary">Registar Alerta</button>
        </div>
    </div>
</TableLayout>

@code {
    [Parameter]
    public int Id { get; set; }

    private string Modelo = "BMW M4 Coupé";
    private string FaseAtual = "Instalação do Sistema de Arrefecimento";
    private int FaseAtualIndex = 1; // Exemplo de fase atual
    private int TotalFases = 7;    // Total de fases do processo
}

<style>
    .encomenda-container {
        padding: 20px;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
        height:92%;
        color: #121212; /* Texto preto para maior visibilidade */
    }

    .encomenda-detalhes {
        margin-bottom: 20px;
    }

    .encomenda-detalhes h3 {
        margin-bottom: 10px;
        color: #007bff; /* Azul para destaque */
    }

    .encomenda-detalhes p {
        margin: 5px 0;
        color: #333333; /* Cinza escuro para maior contraste */
    }

    .encomenda-imagem img {
        width: 100%;
        max-width: 600px;
        border-radius: 10px;
        margin: 20px 0;
    }

    .encomenda-progresso {
        margin: 20px 0;
    }

    .progress-bar {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }

    .progress-step {
        width: 15px;
        height: 15px;
        border-radius: 50%;
        background-color: #cccccc;
    }

    .progress-step.completed {
        background-color: #007bff;
    }

    .encomenda-acoes {
        margin-top: 20px;
    }

    .btn {
        background-color: #007bff;
        color: #ffffff;
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .btn:hover {
        background-color: #0056b3;
    }
</style>
